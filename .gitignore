node_modules/
.expo/
dist/
web-build/
*.log

# Native build output (EAS builds these; don't commit)
/ios
/android
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Secrets — never commit. Only .env.example is tracked.
.env
.env.*
# `.env.*` does not match `anything.env`, and a file named that way is still a
# real environment file — a downloaded or generated one usually is.
*.env
!.env.example

# Supabase local stack
supabase/.branches/
supabase/.temp/

.DS_Store
Thumbs.db

# Source proposal — kept locally, not published.
CardinalSkill.pdf

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# Local QA and tooling scratch — not part of the app.
.scratch/
.playwright-cli/
graphify-out/
.claude/
.impeccable/
