# Cardinal Skill has no Python runtime dependencies.
# The application runs on Expo / React Native, and its server-side syllabus
# parser runs in Deno through Supabase Edge Functions. Keep this file empty
# unless a Python-specific tool or service is deliberately introduced.
