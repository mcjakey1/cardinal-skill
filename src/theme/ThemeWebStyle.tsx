import type { ThemePalette } from './themes';

/** Native scroll indicators are owned by the operating system, not CSS. */
export function ThemeWebStyle(_props: { theme: ThemePalette }) {
  return null;
}
